# Curated Memory

This file is your shared scratch pad for context that isn't tied to a single
user. Edit it as you learn. Keep it short, factual, and current.

## Active projects
(none)

## Standing preferences
(none)

## Operational notes
- Memory storage is at `data/state.db`. Per-user facts live in the `facts` table.
- Skills are at `.claude/skills/`. Edit via `mcp__aether__edit_skill`.
- Schedules are at `data/state.db` `schedules` table. Manage via `mcp__aether__schedule`.
